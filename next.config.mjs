
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost"], // اگر دامنه API دیگه‌ای هم داری، اضافه کن
  },
};

export default nextConfig;
